import 'package:flutter/material.dart';

class SpaceHeight12 extends StatelessWidget {
  const SpaceHeight12({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 12.0,
    );
  }
}

class SpaceHeight4 extends StatelessWidget {
  const SpaceHeight4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 4.0,
    );
  }
}

class SpaceHeight8 extends StatelessWidget {
  const SpaceHeight8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 8.0,
    );
  }
}

class SpaceHeight16 extends StatelessWidget {
  const SpaceHeight16({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 16.0,
    );
  }
}
